{
  "name": "rucalo-be",
  "version": "0.0.1",
  "description": "Password manager",
  "main": "index.ts",
  "scripts": {
    "clean": "rm -rf ./build",
    "build": "tsc",
    "predev": "npm run build",
    "dev": "cross-env ENV='dev' ts-node ./build/index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SoGo",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^6.0.3",
    "express": "^4.17.1",
    "mongodb": "^3.3.2",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.1"
  }
}
